'use client';
import { useState } from 'react';
import Link from 'next/link';
import { createBrowserClient } from '@supabase/ssr';
export default function Login(){const [email,setEmail]=useState(''); const [password,setPassword]=useState(''); const [error,setError]=useState(''); const [busy,setBusy]=useState(false); async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);setError(''); const db=createBrowserClient(process.env.NEXT_PUBLIC_SUPABASE_URL!,process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!);const {error}=await db.auth.signInWithPassword({email,password});if(error){setError('Unable to sign in. Check your details and try again.');setBusy(false)}else window.location.assign('/');}return <div className="auth-wrap"><div className="auth-card"><Link href="/" className="auth-logo"><img src="/acg-logo.jpg" alt="Asset Care Group"/></Link><span className="eyebrow">YOUR PROPERTY PORTAL</span><h1>Welcome back.</h1><p>Sign in to view your property information.</p><form onSubmit={submit}><label htmlFor="email">Email address</label><input id="email" type="email" autoComplete="email" required value={email} onChange={e=>setEmail(e.target.value)}/><label htmlFor="password">Password</label><input id="password" type="password" autoComplete="current-password" required value={password} onChange={e=>setPassword(e.target.value)}/>{error&&<p className="form-error" role="alert">{error}</p>}<button className="button dark full" disabled={busy||!process.env.NEXT_PUBLIC_SUPABASE_URL}>{busy?'Signing in…':'Sign in'}</button></form><Link className="auth-help" href="/reset-password">Forgot your password?</Link></div></div>}
