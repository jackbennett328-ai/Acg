'use client';
import {useState} from 'react';
import Link from 'next/link';
import {createBrowserClient} from '@supabase/ssr';
export default function Reset(){const [email,setEmail]=useState('');const [message,setMessage]=useState('');const [busy,setBusy]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);const db=createBrowserClient(process.env.NEXT_PUBLIC_SUPABASE_URL!,process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!);await db.auth.resetPasswordForEmail(email,{redirectTo:`${window.location.origin}/auth/callback?next=/update-password`});setMessage('If an account exists for that address, a reset link is on its way.');setBusy(false)}return <div className="auth-wrap"><div className="auth-card"><Link className="auth-logo" href="/"><img src="/acg-logo.jpg" alt="Asset Care Group"/></Link><h1>Reset your password.</h1><p>Enter your account email and we’ll send you a secure link.</p><form onSubmit={submit}><label htmlFor="email">Email address</label><input id="email" type="email" autoComplete="email" required value={email} onChange={e=>setEmail(e.target.value)}/><button className="button dark full" disabled={busy}>{busy?'Sending…':'Send reset link'}</button></form>{message&&<p role="status">{message}</p>}<Link className="auth-help" href="/login">Back to sign in</Link></div></div>}
