'use client';
import {useState} from 'react';
import {createBrowserClient} from '@supabase/ssr';
export default function Update(){const [password,setPassword]=useState('');const [message,setMessage]=useState('');async function submit(e:React.FormEvent){e.preventDefault();const db=createBrowserClient(process.env.NEXT_PUBLIC_SUPABASE_URL!,process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!);const {error}=await db.auth.updateUser({password});setMessage(error?'Unable to update password. Please request a new link.':'Password updated. You can now return to your properties.')}return <div className="auth-wrap"><div className="auth-card"><h1>Choose a new password.</h1><form onSubmit={submit}><label htmlFor="password">New password</label><input id="password" type="password" autoComplete="new-password" minLength={12} required value={password} onChange={e=>setPassword(e.target.value)}/><button className="button dark full">Update password</button></form>{message&&<p role="status">{message}</p>}</div></div>}
